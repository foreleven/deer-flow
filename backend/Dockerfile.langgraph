# NOTE: Deprecated / unused Dockerfile for LangGraph backend
#
# This file was originally a generated Dockerfile (`Dockerfile.langgraph`)
# and is not referenced by the current Docker or Compose workflows.
#
# The authoritative Dockerfile for the LangGraph service is:
#   backend/Dockerfile
#
# This stub is kept only to avoid confusion if older references mention
# `backend/Dockerfile.langgraph`. It is safe to delete this file from the
# repository once all such references have been cleaned up.
#
# There is intentionally no buildable image definition here.
